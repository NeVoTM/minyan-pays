{
  "name": "minyan-pays",
  "private": true,
  "version": "0.1.0",
  "description": "Dovrey Evrit — minyan attendance, first-9 payouts, treasury lock",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/api && npm run build --workspace apps/web",
    "db:generate": "npm run db:generate --workspace apps/api",
    "db:migrate": "npm run db:migrate --workspace apps/api",
    "db:push": "npm run db:push --workspace apps/api",
    "tunnel": "npx --yes localtunnel --port 5173"
  },
  "workspaces": [
    "apps/api",
    "apps/web"
  ]
}
