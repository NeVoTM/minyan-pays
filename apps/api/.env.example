# Prisma uses PostgreSQL. Pick one:

# Local Docker (repo root: docker compose up -d) — port 5432:
# DATABASE_URL="postgresql://minyan:minyan@localhost:5432/minyan_pays"

# Local embedded Postgres (no Docker — npm run db:local from repo root) — port 5433:
DATABASE_URL="postgresql://minyan:minyan@127.0.0.1:5433/minyan_pays"

# Free cloud Postgres (Neon — see docs/FREE_DATABASE_NEON.md):
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require"

# Render / other managed Postgres:
# DATABASE_URL="postgresql://..."
# Used for /api/auth/admin when Organization.adminPasswordHash is null.
# After an admin changes the password in the dashboard (Overview), the stored hash is used instead.
# If login fails after a dashboard password change, either use that new password or run
# `npm run db:clear-admin-hash` (with DATABASE_URL) to fall back to this env value again.
# Value may be entered with or without surrounding quotes; both are accepted.
ADMIN_PASSWORD="change-me"
JWT_SECRET="generate-with-openssl-rand-base64-32"
WEB_ORIGIN="http://localhost:5173"
DEFAULT_TIMEZONE="America/New_York"
